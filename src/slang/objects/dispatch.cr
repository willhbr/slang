module Slang
  module Dispatchable
    
  end

  # class SLObject
  #   property type : SLClass
  # end

  # class SLClass < SLObject
  #   property implementations = Hash(Protocol, ProtocolImplementation).new
  #   property methods = Hash(String, Function).new
  # end
end
